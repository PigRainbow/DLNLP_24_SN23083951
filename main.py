import sys
import os

sys.path.append(os.path.join(os.getcwd(), 'GRUs_model'))
sys.path.append(os.path.join(os.getcwd(), 'BERT_model'))

from GRUs_model import GRUs_m
from BERT_model import BERT_m

if __name__ == "__main__":
    pass
